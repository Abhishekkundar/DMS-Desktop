<Window x:Class="DMS.Desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DMS - Login"
        Width="520" Height="610"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F3F5F7"
        FontFamily="Arial">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="BorderBrush" Value="#AEBBC5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="BorderBrush" Value="#AEBBC5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="BorderBrush" Value="#AEBBC5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="95"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#AEBBC5"/>
            <Setter Property="Foreground" Value="#354957"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#315D7C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#294F69"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Border Margin="34" Background="White"
            BorderBrush="#B8C3CC" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#E9EEF2"
                    BorderBrush="#B8C3CC" BorderThickness="0,0,0,1">
                <TextBlock Text="DOCUMENT MANAGEMENT SYSTEM"
                           FontSize="14" FontWeight="SemiBold"
                           Foreground="#263746"
                           VerticalAlignment="Center"
                           Margin="12,0"/>
            </Border>

            <StackPanel Grid.Row="1" Width="350"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <Border Width="52" Height="52"
                        Background="#315D7C"
                        CornerRadius="2"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,12">
                    <TextBlock Text="✦"
                               Foreground="White"
                               FontSize="28"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="Government Document Management"
                           FontSize="12"
                           Foreground="#61717D"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="DOCUMENT MANAGEMENT SYSTEM"
                           FontSize="19"
                           FontWeight="SemiBold"
                           Foreground="#263746"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,22"/>

                <Border Background="#F7F9FA"
                        BorderBrush="#D0D8DE"
                        BorderThickness="1"
                        Padding="20">
                    <StackPanel>
                        <TextBlock Text="Administrator Login"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="#315D7C"
                                   Margin="0,0,0,14"/>

                        <TextBlock Text="User ID"
                                   FontSize="13"
                                   Foreground="#374957"
                                   Margin="0,0,0,5"/>
                        <TextBox x:Name="UserIdBox" Text="admin"/>

                        <TextBlock Text="Password"
                                   FontSize="13"
                                   Foreground="#374957"
                                   Margin="0,13,0,5"/>
                        <PasswordBox x:Name="PasswordBox"/>

                        <TextBlock Text="Language"
                                   FontSize="13"
                                   Foreground="#374957"
                                   Margin="0,13,0,5"/>
                        <ComboBox x:Name="LanguageBox" SelectedIndex="0">
                            <ComboBoxItem Content="English"/>
                            <ComboBoxItem Content="ಕನ್ನಡ"/>
                        </ComboBox>

                        <TextBlock x:Name="ErrorText"
                                   Text=""
                                   Foreground="#B3261E"
                                   FontSize="12"
                                   Margin="0,10,0,0"
                                   TextWrapping="Wrap"/>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Margin="0,18,0,0">
                            <Button Style="{StaticResource PrimaryButton}"
                                    Content="Login"
                                    Click="Login_Click"/>
                            <Button Content="Exit"
                                    Click="Exit_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <TextBlock Grid.Row="2"
                       Text="DMS | Version 1.0.0"
                       FontSize="11"
                       Foreground="#61717D"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
