<Window x:Class="DMS.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DOCUMENT MANAGEMENT SYSTEM"
        Width="1250" Height="760"
        MinWidth="1050" MinHeight="650"
        WindowStartupLocation="CenterScreen"
        Background="#F3F5F7"
        FontFamily="Arial">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#AEBBC5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#354957"/>
        </Style>

        <Style x:Key="ToolbarButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="42"/>
            <Setter Property="MinWidth" Value="86"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#315D7C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#294F69"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="7,4"/>
            <Setter Property="BorderBrush" Value="#AEBBC5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderBrush" Value="#AEBBC5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#374957"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,5,0,3"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#C6D0D8" BorderThickness="0,0,0,1">
            <Grid Margin="16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="34" Height="34" Background="#315D7C" CornerRadius="2" Margin="0,0,10,0">
                        <TextBlock Text="✦" Foreground="White" FontSize="20"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="DOCUMENT MANAGEMENT SYSTEM"
                                   FontSize="17" FontWeight="SemiBold" Foreground="#263746"/>
                        <TextBlock Text="Government Document Management"
                                   FontSize="11" Foreground="#61717D" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Administrator: " Foreground="#526675" VerticalAlignment="Center"/>
                    <TextBlock Text="admin" FontWeight="Bold" Foreground="#263746" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <ComboBox Width="105" SelectedIndex="0">
                        <ComboBoxItem Content="English"/>
                        <ComboBoxItem Content="ಕನ್ನಡ"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#E9EEF2" BorderBrush="#C6D0D8" BorderThickness="0,0,0,1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal" Margin="10,3">
                    <Button Style="{StaticResource ToolbarButton}" Content="👤  Create User" Click="CreateUser_Click"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="👤  Delete User"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="🔎  Search" Click="Search_Click"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="▣  Index"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="↑  Upload"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="▤  Excel"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="✕  Delete"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="✎  Edit"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="⇥  Logout" Click="Logout_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Work area -->
        <Grid Grid.Row="2" Margin="14">
            <Border Background="White" BorderBrush="#C6D0D8" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#E9EEF2" BorderBrush="#B8C3CC" BorderThickness="0,0,0,1">
                        <TextBlock x:Name="SectionTitle" Text="Document Management System"
                                   FontWeight="SemiBold" Foreground="#263746"
                                   VerticalAlignment="Center" Margin="12,0"/>
                    </Border>

                    <Grid Grid.Row="1" Margin="18">
                        <!-- Home -->
                        <StackPanel x:Name="HomePanel" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="Document Management System" FontSize="22"
                                       FontWeight="SemiBold" Foreground="#315D7C"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Select an option from the toolbar"
                                       FontSize="14" Foreground="#657682"
                                       Margin="0,8,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Search -->
                        <Grid x:Name="SearchPanel" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <UniformGrid Rows="2" Columns="4" Margin="0,0,0,10">
                                <StackPanel Margin="5"><Label Content="Project Code"/><TextBox/></StackPanel>
                                <StackPanel Margin="5"><Label Content="Unit"/><ComboBox><ComboBoxItem Content="--All--"/><ComboBoxItem Content="Finance"/><ComboBoxItem Content="Works"/></ComboBox></StackPanel>
                                <StackPanel Margin="5"><Label Content="Year"/><ComboBox><ComboBoxItem Content="--All--"/><ComboBoxItem Content="2026"/></ComboBox></StackPanel>
                                <StackPanel Margin="5"><Label Content="Month"/><ComboBox><ComboBoxItem Content="--All--"/><ComboBoxItem Content="September"/></ComboBox></StackPanel>
                                <StackPanel Margin="5"><Label Content="Voucher Type"/><ComboBox><ComboBoxItem Content="--All--"/><ComboBoxItem Content="Invoice"/></ComboBox></StackPanel>
                                <StackPanel Margin="5"><Label Content="Name / Description"/><TextBox/></StackPanel>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
                                    <Button Style="{StaticResource PrimaryButton}" Content="Search" Width="90"/>
                                    <Button Content="Clear" Width="75"/>
                                </StackPanel>
                            </UniformGrid>

                            <TextBlock Grid.Row="1" Text="Search Results" FontSize="15"
                                       FontWeight="SemiBold" Foreground="#354957" Margin="5,5,0,8"/>

                            <DataGrid Grid.Row="2" AutoGenerateColumns="False"
                                      HeadersVisibility="Column" CanUserAddRows="False"
                                      IsReadOnly="True" BorderBrush="#AEBBC5"
                                      RowHeight="34" FontSize="12">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Sl.No" Binding="{Binding SlNo}" Width="55"/>
                                    <DataGridTextColumn Header="Project Code" Binding="{Binding ProjectCode}" Width="100"/>
                                    <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="115"/>
                                    <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="70"/>
                                    <DataGridTextColumn Header="Month" Binding="{Binding Month}" Width="90"/>
                                    <DataGridTextColumn Header="Voucher Type" Binding="{Binding VoucherType}" Width="100"/>
                                    <DataGridTextColumn Header="Name / Description" Binding="{Binding Description}" Width="*"/>
                                    <DataGridTemplateColumn Header="Action" Width="230">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="View" Padding="9,4" Click="View_Click"/>
                                                    <Button Content="Reply" Padding="9,4" Click="Reply_Click"/>
                                                    <Button Content="Print" Padding="9,4" Click="Print_Click"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>

                        <!-- Reply -->
                        <Grid x:Name="ReplyPanel" Visibility="Collapsed">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel MaxWidth="1050">
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="5">
                                            <Label Content="Request No."/>
                                            <TextBox Text="REQ-2026-00125"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="5">
                                            <Label Content="Request Date"/>
                                            <TextBox Text="08/09/2026"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Margin="5">
                                        <Label Content="Document No."/>
                                        <TextBox Text="DOC-2026-00458"/>
                                    </StackPanel>

                                    <StackPanel Margin="5">
                                        <Label Content="To *"/>
                                        <TextBox x:Name="ReplyToBox"/>
                                    </StackPanel>

                                    <StackPanel Margin="5">
                                        <Label Content="Address"/>
                                        <TextBox Height="70" AcceptsReturn="True" TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <StackPanel Margin="5">
                                        <Label Content="Subject"/>
                                        <TextBox/>
                                    </StackPanel>

                                    <StackPanel Margin="5">
                                        <Label Content="Reference"/>
                                        <TextBox/>
                                    </StackPanel>

                                    <StackPanel Margin="5">
                                        <Label Content="Reply *"/>
                                        <TextBox Height="210" AcceptsReturn="True"
                                                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                                 x:Name="ReplyTextBox"/>
                                    </StackPanel>

                                    <StackPanel Margin="5">
                                        <Label Content="Attachment"/>
                                        <Button Content="Choose File" Width="110" HorizontalAlignment="Left"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5,15,5,5">
                                        <Button Content="Clear" Width="85" Click="ReplyClear_Click"/>
                                        <Button Style="{StaticResource PrimaryButton}" Content="Reply" Width="85" Click="ReplySubmit_Click"/>
                                        <Button Content="🖨 Print" Width="95" Click="Print_Click"/>
                                    </StackPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>

                        <!-- Create User -->
                        <Grid x:Name="CreateUserPanel" Visibility="Collapsed">
                            <StackPanel MaxWidth="650">
                                <Label Content="Create Account" FontSize="15" FontWeight="SemiBold"/>
                                <UniformGrid Columns="2" Margin="0,5">
                                    <Label Content="Name"/><TextBox/>
                                    <Label Content="Employee ID"/><TextBox/>
                                    <Label Content="User ID"/><TextBox/>
                                    <Label Content="Password"/><PasswordBox Margin="0,5"/>
                                </UniformGrid>
                                <GroupBox Header="Permissions" Margin="5,10">
                                    <UniformGrid Columns="2" Margin="10">
                                        <CheckBox Content="Create User" Margin="5"/>
                                        <CheckBox Content="Delete User" Margin="5"/>
                                        <CheckBox Content="Search" Margin="5"/>
                                        <CheckBox Content="Index" Margin="5"/>
                                        <CheckBox Content="Upload" Margin="5"/>
                                        <CheckBox Content="Excel Report" Margin="5"/>
                                        <CheckBox Content="Edit" Margin="5"/>
                                        <CheckBox Content="Delete" Margin="5"/>
                                    </UniformGrid>
                                </GroupBox>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Style="{StaticResource PrimaryButton}" Content="Create" Width="90"/>
                                    <Button Content="Clear" Width="90"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Status -->
        <Border Grid.Row="3" Background="#E9EEF2" BorderBrush="#C6D0D8" BorderThickness="0,1,0,0">
            <TextBlock Text="DMS  |  Administrator: admin  |  Ready"
                       Foreground="#526675" FontSize="11" VerticalAlignment="Center" Margin="12,0"/>
        </Border>
    </Grid>
</Window>
